apiVersion: v1
kind : Pod
metadata: 
  name: postgres-app-pod
  labels:
    name: postgres-app-pod
    app: demo-voting-app

spec:
  conatainers:
  - name: postgres-app
    image: postgres:9.4
    ports:
    - conatainerPort: 5432