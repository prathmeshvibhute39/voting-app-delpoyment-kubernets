apiVersion: v1
kind : Pod
metadata: 
  name: redis-app-pod
  labels:
    name: redis-app-pod
    app: demo-voting-app

spec:
  conatainers:
  - name: redis-app
    image: redis
    ports:
    - conatainerPort: 6379